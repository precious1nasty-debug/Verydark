import { n as createMiddleware } from "./ssr.mjs";
import { n as clsx } from "../_libs/class-variance-authority+clsx.mjs";
import { t as twMerge } from "../_libs/tailwind-merge.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/utils-D5lQuapZ.js
var _0001_auth_default = "-- Better Auth schema (identity + sessions for \"Sign in with Grok\").\n--\n-- Generated by the Better Auth CLI for its Postgres adapter — DO NOT EDIT by\n-- hand. `@/lib/auth/server` runs Better Auth against these tables when\n-- DATABASE_URL is set. The columns are camelCase and MUST stay double-quoted so\n-- Postgres preserves the case Better Auth queries by.\n--\n-- Migrations in this folder are the single source of truth for your schema. They\n-- apply to Neon during the Vercel build (`npm run build`) and to the local\n-- PGLite fallback automatically on startup, so dev matches production. Applied\n-- files are recorded by name in `_migrations` and NEVER run again.\n--\n-- Put YOUR app's schema in NEW ordered files (0002_*.sql, 0003_*.sql, …), never\n-- in this one. For app tables, prefer snake_case and give per-user tables a\n-- `user_id TEXT NOT NULL` column (TEXT, not UUID — the preview dev user id is\n-- the string 'dev-user'), then scope every query to the authenticated user\n-- server-side (see the `neon` + `auth` skills and src/lib/auth/verify.server.ts).\n\ncreate table if not exists \"user\" (\n  \"id\" text not null primary key,\n  \"name\" text not null,\n  \"email\" text not null unique,\n  \"emailVerified\" boolean not null,\n  \"image\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate table if not exists \"session\" (\n  \"id\" text not null primary key,\n  \"expiresAt\" timestamptz not null,\n  \"token\" text not null unique,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null,\n  \"ipAddress\" text,\n  \"userAgent\" text,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade\n);\n\ncreate table if not exists \"account\" (\n  \"id\" text not null primary key,\n  \"accountId\" text not null,\n  \"providerId\" text not null,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade,\n  \"accessToken\" text,\n  \"refreshToken\" text,\n  \"idToken\" text,\n  \"accessTokenExpiresAt\" timestamptz,\n  \"refreshTokenExpiresAt\" timestamptz,\n  \"scope\" text,\n  \"password\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null\n);\n\ncreate table if not exists \"verification\" (\n  \"id\" text not null primary key,\n  \"identifier\" text not null,\n  \"value\" text not null,\n  \"expiresAt\" timestamptz not null,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate index if not exists \"session_userId_idx\" on \"session\" (\"userId\");\ncreate index if not exists \"account_userId_idx\" on \"account\" (\"userId\");\ncreate index if not exists \"verification_identifier_idx\" on \"verification\" (\"identifier\");\n";
var _0002_league_default = "-- Dream League schema\n\ncreate table if not exists league_meta (\n  id          integer primary key default 1 check (id = 1),\n  name        text not null,\n  season      text not null,\n  tagline     text not null default '',\n  about       text not null default '',\n  updated_at  timestamptz not null default now()\n);\n\ncreate table if not exists league_admins (\n  user_id     text primary key,\n  created_at  timestamptz not null default now()\n);\n\ncreate table if not exists teams (\n  id              serial primary key,\n  name            text not null,\n  short_name      text not null,\n  slug            text not null unique,\n  primary_color   text not null,\n  secondary_color text not null,\n  coach           text not null default '',\n  founded         text not null default '',\n  stadium         text not null default '',\n  created_at      timestamptz not null default now()\n);\n\ncreate table if not exists players (\n  id          serial primary key,\n  team_id     integer not null references teams(id) on delete cascade,\n  name        text not null,\n  shirt_number integer not null,\n  position    text not null check (position in ('GK', 'DF', 'MF', 'FW')),\n  created_at  timestamptz not null default now(),\n  unique (team_id, shirt_number)\n);\n\ncreate table if not exists matches (\n  id            serial primary key,\n  matchday      integer not null,\n  home_team_id  integer not null references teams(id),\n  away_team_id  integer not null references teams(id),\n  kickoff_at    timestamptz not null,\n  venue         text not null default '',\n  status        text not null default 'upcoming' check (status in ('upcoming', 'live', 'finished')),\n  home_score    integer not null default 0,\n  away_score    integer not null default 0,\n  created_at    timestamptz not null default now(),\n  updated_at    timestamptz not null default now(),\n  check (home_team_id <> away_team_id),\n  check (home_score >= 0 and away_score >= 0)\n);\n\ncreate table if not exists match_events (\n  id                serial primary key,\n  match_id          integer not null references matches(id) on delete cascade,\n  team_id           integer not null references teams(id),\n  player_id         integer references players(id) on delete set null,\n  assist_player_id  integer references players(id) on delete set null,\n  event_type        text not null check (event_type in ('goal', 'own_goal', 'yellow', 'red')),\n  minute            integer not null check (minute between 1 and 120),\n  created_at        timestamptz not null default now()\n);\n\ncreate table if not exists announcements (\n  id            serial primary key,\n  title         text not null,\n  body          text not null,\n  published_at  timestamptz not null default now(),\n  created_at    timestamptz not null default now()\n);\n\ncreate index if not exists players_team_id_idx on players (team_id);\ncreate index if not exists matches_status_kickoff_idx on matches (status, kickoff_at);\ncreate index if not exists matches_matchday_idx on matches (matchday);\ncreate index if not exists match_events_match_id_idx on match_events (match_id);\ncreate index if not exists match_events_player_id_idx on match_events (player_id);\ncreate index if not exists announcements_published_idx on announcements (published_at desc);\n";
var _0003_seed_default = "-- Dream League sample data\n\ninsert into league_meta (id, name, season, tagline, about) values (\n  1,\n  'Dream League',\n  '2026',\n  'Ten clubs. One table. Every match counts.',\n  'Dream League is an independent football championship built around ten ambitious clubs. The format is a single round-robin: each side plays every other side once across nine matchdays. Three points for a win, one for a draw, and goal difference as the first tie-breaker. The competition exists to give local sides a proper stage — fixtures, live scores, standings and player records, all in one place.'\n);\n\ninsert into teams (id, name, short_name, slug, primary_color, secondary_color, coach, founded, stadium) values\n  (1, 'Talent FC', 'TAL', 'talent-fc', '#1B4B5A', '#7EC8B8', 'Adrian Cole', '2019', 'Talent Park'),\n  (2, 'Pluto FC', 'PLU', 'pluto-fc', '#1C2430', '#8AA0B8', 'Nora Veld', '2020', 'Orbit Arena'),\n  (3, 'Precious FC', 'PRE', 'precious-fc', '#5C2A36', '#E4D4C8', 'Imani Rhodes', '2018', 'Pearl Ground'),\n  (4, 'Santos FC', 'SAN', 'santos-fc', '#141414', '#F2F2F0', 'Mateo Ruiz', '2016', 'Santos Stadium'),\n  (5, 'David FC', 'DAV', 'david-fc', '#1E4D8C', '#D6E4F5', 'Leah Okoye', '2021', 'David Field'),\n  (6, 'Myles FC', 'MYL', 'myles-fc', '#1C4A3C', '#A8D5C2', 'Jonas Pike', '2017', 'Myles Park'),\n  (7, 'SALVA FC', 'SAL', 'salva-fc', '#7A1F2B', '#F0D5D8', 'Rafael Costa', '2015', 'SALVA Arena'),\n  (8, 'Gentle FC', 'GEN', 'gentle-fc', '#3A5A7C', '#C9D6E4', 'Sofia Hart', '2022', 'Harbor Park'),\n  (9, 'Big Perezi FC', 'BPZ', 'big-perezi-fc', '#2A2A28', '#C48A5A', 'Perezi Kane', '2014', 'Perezi Stadium'),\n  (10, 'BIG Enzo FC', 'ENZ', 'big-enzo-fc', '#0F4C5C', '#9FD6D2', 'Enzo Marlowe', '2023', 'Enzo Ground');\nselect setval('teams_id_seq', 10);\n\ninsert into players (id, team_id, name, shirt_number, position) values\n  (1, 1, 'Jordan Blake', 1, 'GK'),\n  (2, 1, 'Malik Okonkwo', 2, 'DF'),\n  (3, 1, 'Rico Alvarez', 3, 'DF'),\n  (4, 1, 'Theo Marin', 4, 'DF'),\n  (5, 1, 'Samuel Ibe', 5, 'DF'),\n  (6, 1, 'Nabil Farouk', 6, 'MF'),\n  (7, 1, 'Callum Reed', 8, 'MF'),\n  (8, 1, 'Elias Grant', 10, 'MF'),\n  (9, 1, 'Yuki Sato', 7, 'MF'),\n  (10, 1, 'Marcus Hale', 9, 'FW'),\n  (11, 1, 'Kai Brennan', 11, 'FW'),\n  (12, 1, 'Omar Sylla', 14, 'FW'),\n  (13, 1, 'Luis Navarro', 17, 'FW'),\n  (14, 2, 'Henrik Voss', 1, 'GK'),\n  (15, 2, 'Arlo Finch', 2, 'DF'),\n  (16, 2, 'Kian Rowe', 3, 'DF'),\n  (17, 2, 'Darius Holm', 4, 'DF'),\n  (18, 2, 'Nico Bell', 5, 'DF'),\n  (19, 2, 'Soren Blake', 6, 'MF'),\n  (20, 2, 'Ivy Chen', 8, 'MF'),\n  (21, 2, 'Rafael Dusk', 10, 'MF'),\n  (22, 2, 'Owen Hart', 7, 'MF'),\n  (23, 2, 'Liam Frost', 9, 'FW'),\n  (24, 2, 'Zara Quinn', 11, 'FW'),\n  (25, 2, 'Felix Nord', 14, 'FW'),\n  (26, 2, 'Tomas Grey', 19, 'FW'),\n  (27, 3, 'Amara Cole', 1, 'GK'),\n  (28, 3, 'Julian Vos', 2, 'DF'),\n  (29, 3, 'Eden Park', 3, 'DF'),\n  (30, 3, 'Micah Stone', 4, 'DF'),\n  (31, 3, 'Rowan Ellis', 5, 'DF'),\n  (32, 3, 'Sable Ward', 6, 'MF'),\n  (33, 3, 'Nina Okada', 8, 'MF'),\n  (34, 3, 'Leo Prest', 10, 'MF'),\n  (35, 3, 'Hugo Lane', 7, 'MF'),\n  (36, 3, 'Aria Bellamy', 9, 'FW'),\n  (37, 3, 'Caleb Roux', 11, 'FW'),\n  (38, 3, 'Ivy Moreau', 14, 'FW'),\n  (39, 3, 'Seth Kline', 21, 'FW'),\n  (40, 4, 'Paulo Mendes', 1, 'GK'),\n  (41, 4, 'Bruno Silva', 2, 'DF'),\n  (42, 4, 'Hector Vale', 3, 'DF'),\n  (43, 4, 'Andre Costa', 4, 'DF'),\n  (44, 4, 'Rui Palmeiro', 5, 'DF'),\n  (45, 4, 'Diego Mora', 6, 'MF'),\n  (46, 4, 'Carlos Nunez', 8, 'MF'),\n  (47, 4, 'Mateo Cruz', 10, 'MF'),\n  (48, 4, 'Luka Perez', 7, 'MF'),\n  (49, 4, 'Rafa Santos', 9, 'FW'),\n  (50, 4, 'Joao Vidal', 11, 'FW'),\n  (51, 4, 'Nico Barros', 14, 'FW'),\n  (52, 4, 'Alex Romero', 22, 'FW'),\n  (53, 5, 'Noah Adeyemi', 1, 'GK'),\n  (54, 5, 'Ben Carter', 2, 'DF'),\n  (55, 5, 'Kyle Davenport', 3, 'DF'),\n  (56, 5, 'Omar Hassan', 4, 'DF'),\n  (57, 5, 'Will Fraser', 5, 'DF'),\n  (58, 5, 'James Okoro', 6, 'MF'),\n  (59, 5, 'Tyler Dean', 8, 'MF'),\n  (60, 5, 'Chris Lang', 10, 'MF'),\n  (61, 5, 'Ethan Brooks', 7, 'MF'),\n  (62, 5, 'David Kim', 9, 'FW'),\n  (63, 5, 'Ryan Cole', 11, 'FW'),\n  (64, 5, 'Alex Mbeki', 14, 'FW'),\n  (65, 5, 'Sam Irons', 18, 'FW'),\n  (66, 6, 'Pete Holloway', 1, 'GK'),\n  (67, 6, 'Gareth Moon', 2, 'DF'),\n  (68, 6, 'Ian Brooks', 3, 'DF'),\n  (69, 6, 'Owen Dale', 4, 'DF'),\n  (70, 6, 'Harry Finch', 5, 'DF'),\n  (71, 6, 'Miles Turner', 6, 'MF'),\n  (72, 6, 'Connor Shaw', 8, 'MF'),\n  (73, 6, 'Reece Quinn', 10, 'MF'),\n  (74, 6, 'Adam Pike', 7, 'MF'),\n  (75, 6, 'Myles Harbor', 9, 'FW'),\n  (76, 6, 'Finn Adler', 11, 'FW'),\n  (77, 6, 'Josh Wren', 14, 'FW'),\n  (78, 6, 'Leo Marsh', 16, 'FW'),\n  (79, 7, 'Marco Silva', 1, 'GK'),\n  (80, 7, 'Ivan Petrov', 2, 'DF'),\n  (81, 7, 'Luis Ortega', 3, 'DF'),\n  (82, 7, 'Pavel Kovac', 4, 'DF'),\n  (83, 7, 'Dante Ruiz', 5, 'DF'),\n  (84, 7, 'Sergio Vale', 6, 'MF'),\n  (85, 7, 'Andrei Popa', 8, 'MF'),\n  (86, 7, 'Rafael Costa Jr', 10, 'MF'),\n  (87, 7, 'Nico Salva', 7, 'MF'),\n  (88, 7, 'Diego Hart', 9, 'FW'),\n  (89, 7, 'Emil Vargas', 11, 'FW'),\n  (90, 7, 'Tomas Rivera', 14, 'FW'),\n  (91, 7, 'Alexei Dorn', 20, 'FW'),\n  (92, 8, 'Oliver Shaw', 1, 'GK'),\n  (93, 8, 'Henry Clarke', 2, 'DF'),\n  (94, 8, 'Edward Moss', 3, 'DF'),\n  (95, 8, 'Arthur Vale', 4, 'DF'),\n  (96, 8, 'George Lin', 5, 'DF'),\n  (97, 8, 'William Hart', 6, 'MF'),\n  (98, 8, 'Charles Reed', 8, 'MF'),\n  (99, 8, 'Frederick Lane', 10, 'MF'),\n  (100, 8, 'Louis Grey', 7, 'MF'),\n  (101, 8, 'Gentle Hale', 9, 'FW'),\n  (102, 8, 'Patrick Snow', 11, 'FW'),\n  (103, 8, 'Vincent Cole', 14, 'FW'),\n  (104, 8, 'Hugh Bell', 15, 'FW'),\n  (105, 9, 'Biggs Miller', 1, 'GK'),\n  (106, 9, 'Kane Watts', 2, 'DF'),\n  (107, 9, 'Troy Dunn', 3, 'DF'),\n  (108, 9, 'Rex Palmer', 4, 'DF'),\n  (109, 9, 'Cole Bryant', 5, 'DF'),\n  (110, 9, 'Max Perezi', 6, 'MF'),\n  (111, 9, 'Jett Rowe', 8, 'MF'),\n  (112, 9, 'Blaze Ortega', 10, 'MF'),\n  (113, 9, 'Cruz Nolan', 7, 'MF'),\n  (114, 9, 'Perezi Jr', 9, 'FW'),\n  (115, 9, 'Axel Storm', 11, 'FW'),\n  (116, 9, 'Dax Hunter', 14, 'FW'),\n  (117, 9, 'Rico West', 23, 'FW'),\n  (118, 10, 'Enzo Gale', 1, 'GK'),\n  (119, 10, 'Milo Crane', 2, 'DF'),\n  (120, 10, 'Theo Banks', 3, 'DF'),\n  (121, 10, 'Nash Porter', 4, 'DF'),\n  (122, 10, 'Quinn Adler', 5, 'DF'),\n  (123, 10, 'Leo Enzo', 6, 'MF'),\n  (124, 10, 'Jasper Holt', 8, 'MF'),\n  (125, 10, 'Rocco Vale', 10, 'MF'),\n  (126, 10, 'Finn Costa', 7, 'MF'),\n  (127, 10, 'Big Enzo', 9, 'FW'),\n  (128, 10, 'Kai Marlowe', 11, 'FW'),\n  (129, 10, 'Otis Wren', 14, 'FW'),\n  (130, 10, 'Rex Solano', 27, 'FW');\nselect setval('players_id_seq', 130);\n\ninsert into matches (id, matchday, home_team_id, away_team_id, kickoff_at, venue, status, home_score, away_score) values\n  (1, 1, 1, 10, '2026-08-08 14:00:00+00', 'Talent Park', 'finished', 3, 2),\n  (2, 1, 2, 9, '2026-08-08 14:00:00+00', 'Orbit Arena', 'finished', 1, 0),\n  (3, 1, 3, 8, '2026-08-08 16:00:00+00', 'Pearl Ground', 'finished', 1, 1),\n  (4, 1, 4, 7, '2026-08-08 16:00:00+00', 'Santos Stadium', 'finished', 1, 0),\n  (5, 1, 5, 6, '2026-08-08 18:00:00+00', 'David Field', 'finished', 0, 1),\n  (6, 2, 10, 9, '2026-08-15 14:00:00+00', 'Enzo Ground', 'finished', 1, 0),\n  (7, 2, 8, 1, '2026-08-15 14:00:00+00', 'Harbor Park', 'finished', 0, 0),\n  (8, 2, 7, 2, '2026-08-15 16:00:00+00', 'SALVA Arena', 'finished', 2, 2),\n  (9, 2, 6, 3, '2026-08-15 16:00:00+00', 'Myles Park', 'finished', 3, 2),\n  (10, 2, 5, 4, '2026-08-15 18:00:00+00', 'David Field', 'finished', 1, 1),\n  (11, 3, 8, 10, '2026-08-22 14:00:00+00', 'Harbor Park', 'finished', 1, 0),\n  (12, 3, 9, 7, '2026-08-22 14:00:00+00', 'Perezi Stadium', 'finished', 2, 2),\n  (13, 3, 1, 6, '2026-08-22 16:00:00+00', 'Talent Park', 'finished', 0, 0),\n  (14, 3, 2, 5, '2026-08-22 16:00:00+00', 'Orbit Arena', 'finished', 2, 0),\n  (15, 3, 3, 4, '2026-08-22 18:00:00+00', 'Pearl Ground', 'finished', 2, 1),\n  (16, 4, 10, 7, '2026-08-29 14:00:00+00', 'Enzo Ground', 'finished', 2, 1),\n  (17, 4, 6, 8, '2026-08-29 14:00:00+00', 'Myles Park', 'finished', 2, 0),\n  (18, 4, 5, 9, '2026-08-29 16:00:00+00', 'David Field', 'finished', 3, 2),\n  (19, 4, 4, 1, '2026-08-29 16:00:00+00', 'Santos Stadium', 'finished', 1, 1),\n  (20, 4, 3, 2, '2026-08-29 18:00:00+00', 'Pearl Ground', 'finished', 2, 2),\n  (21, 5, 6, 10, '2026-09-02 09:00:00+00', 'Myles Park', 'live', 1, 1),\n  (22, 5, 7, 5, '2026-09-05 14:00:00+00', 'SALVA Arena', 'upcoming', 0, 0),\n  (23, 5, 8, 4, '2026-09-05 16:00:00+00', 'Harbor Park', 'upcoming', 0, 0),\n  (24, 5, 9, 3, '2026-09-05 16:00:00+00', 'Perezi Stadium', 'upcoming', 0, 0),\n  (25, 5, 1, 2, '2026-09-05 18:00:00+00', 'Talent Park', 'upcoming', 0, 0),\n  (26, 6, 10, 5, '2026-09-12 14:00:00+00', 'Enzo Ground', 'upcoming', 0, 0),\n  (27, 6, 4, 6, '2026-09-12 14:00:00+00', 'Santos Stadium', 'upcoming', 0, 0),\n  (28, 6, 3, 7, '2026-09-12 16:00:00+00', 'Pearl Ground', 'upcoming', 0, 0),\n  (29, 6, 2, 8, '2026-09-12 16:00:00+00', 'Orbit Arena', 'upcoming', 0, 0),\n  (30, 6, 1, 9, '2026-09-12 18:00:00+00', 'Talent Park', 'upcoming', 0, 0),\n  (31, 7, 4, 10, '2026-09-19 14:00:00+00', 'Santos Stadium', 'upcoming', 0, 0),\n  (32, 7, 5, 3, '2026-09-19 14:00:00+00', 'David Field', 'upcoming', 0, 0),\n  (33, 7, 6, 2, '2026-09-19 16:00:00+00', 'Myles Park', 'upcoming', 0, 0),\n  (34, 7, 7, 1, '2026-09-19 16:00:00+00', 'SALVA Arena', 'upcoming', 0, 0),\n  (35, 7, 8, 9, '2026-09-19 18:00:00+00', 'Harbor Park', 'upcoming', 0, 0),\n  (36, 8, 10, 3, '2026-09-26 14:00:00+00', 'Enzo Ground', 'upcoming', 0, 0),\n  (37, 8, 2, 4, '2026-09-26 14:00:00+00', 'Orbit Arena', 'upcoming', 0, 0),\n  (38, 8, 1, 5, '2026-09-26 16:00:00+00', 'Talent Park', 'upcoming', 0, 0),\n  (39, 8, 9, 6, '2026-09-26 16:00:00+00', 'Perezi Stadium', 'upcoming', 0, 0),\n  (40, 8, 8, 7, '2026-09-26 18:00:00+00', 'Harbor Park', 'upcoming', 0, 0),\n  (41, 9, 2, 10, '2026-10-03 14:00:00+00', 'Orbit Arena', 'upcoming', 0, 0),\n  (42, 9, 3, 1, '2026-10-03 14:00:00+00', 'Pearl Ground', 'upcoming', 0, 0),\n  (43, 9, 4, 9, '2026-10-03 16:00:00+00', 'Santos Stadium', 'upcoming', 0, 0),\n  (44, 9, 5, 8, '2026-10-03 16:00:00+00', 'David Field', 'upcoming', 0, 0),\n  (45, 9, 6, 7, '2026-10-03 18:00:00+00', 'Myles Park', 'upcoming', 0, 0);\nselect setval('matches_id_seq', 45);\n\ninsert into match_events (id, match_id, team_id, player_id, assist_player_id, event_type, minute) values\n  (1, 1, 1, 13, 6, 'goal', 75),\n  (2, 1, 1, 12, 10, 'goal', 56),\n  (3, 1, 1, 6, null, 'goal', 9),\n  (4, 1, 10, 125, 123, 'goal', 84),\n  (5, 1, 10, 123, 125, 'goal', 81),\n  (6, 1, 1, 3, null, 'yellow', 9),\n  (7, 1, 1, 11, null, 'red', 78),\n  (8, 2, 2, 113, null, 'own_goal', 26),\n  (9, 3, 3, 36, 33, 'goal', 68),\n  (10, 3, 8, 101, null, 'goal', 8),\n  (11, 4, 4, 52, 51, 'goal', 86),\n  (12, 4, 7, 84, null, 'yellow', 69),\n  (13, 5, 6, 77, 78, 'goal', 17),\n  (14, 5, 6, 67, null, 'yellow', 79),\n  (15, 6, 10, 124, 129, 'goal', 52),\n  (16, 8, 7, 87, 90, 'goal', 75),\n  (17, 8, 7, 90, 85, 'goal', 67),\n  (18, 8, 2, 20, 21, 'goal', 26),\n  (19, 8, 2, 22, 25, 'goal', 22),\n  (20, 9, 6, 73, 71, 'goal', 16),\n  (21, 9, 6, 72, 77, 'goal', 40),\n  (22, 9, 6, 78, 71, 'goal', 19),\n  (23, 9, 3, 37, 39, 'goal', 7),\n  (24, 9, 3, 38, 35, 'goal', 31),\n  (25, 9, 6, 73, null, 'yellow', 12),\n  (26, 10, 5, 60, null, 'goal', 12),\n  (27, 10, 4, 52, 46, 'goal', 31),\n  (28, 10, 4, 45, null, 'red', 68),\n  (29, 11, 8, 128, null, 'own_goal', 43),\n  (30, 11, 10, 120, null, 'yellow', 67),\n  (31, 12, 9, 112, null, 'goal', 61),\n  (32, 12, 9, 112, 114, 'goal', 38),\n  (33, 12, 7, 85, 88, 'goal', 62),\n  (34, 12, 7, 89, null, 'goal', 43),\n  (35, 12, 7, 83, null, 'yellow', 13),\n  (36, 13, 6, 72, null, 'yellow', 67),\n  (37, 14, 2, 19, null, 'goal', 72),\n  (38, 14, 2, 24, null, 'goal', 80),\n  (39, 14, 2, 17, null, 'yellow', 78),\n  (40, 15, 3, 33, 39, 'goal', 15),\n  (41, 15, 3, 33, 34, 'goal', 27),\n  (42, 15, 4, 51, 50, 'goal', 19),\n  (43, 15, 3, 27, null, 'yellow', 35),\n  (44, 16, 10, 130, 126, 'goal', 8),\n  (45, 16, 10, 129, null, 'goal', 22),\n  (46, 16, 7, 88, 89, 'goal', 29),\n  (47, 17, 6, 71, 77, 'goal', 20),\n  (48, 17, 6, 73, null, 'goal', 87),\n  (49, 18, 5, 64, null, 'goal', 30),\n  (50, 18, 5, 63, null, 'goal', 86),\n  (51, 18, 5, 58, 63, 'goal', 33),\n  (52, 18, 9, 114, 116, 'goal', 66),\n  (53, 18, 9, 116, 115, 'goal', 46),\n  (54, 19, 4, 5, null, 'own_goal', 89),\n  (55, 19, 1, 6, 9, 'goal', 65),\n  (56, 19, 4, 42, null, 'yellow', 33),\n  (57, 20, 3, 35, null, 'goal', 67),\n  (58, 20, 3, 35, 37, 'goal', 42),\n  (59, 20, 2, 22, 21, 'goal', 14),\n  (60, 20, 2, 19, 22, 'goal', 28),\n  (61, 20, 3, 37, null, 'yellow', 51),\n  (62, 21, 6, 74, 71, 'goal', 18),\n  (63, 21, 10, 125, 124, 'goal', 34),\n  (64, 21, 10, 122, null, 'yellow', 41);\nselect setval('match_events_id_seq', 64);\n\ninsert into announcements (id, title, body, published_at) values\n  (1, 'Dream League 2026 is underway', 'The opening matchday is in the books. Ten clubs, nine rounds, and a table that will not stay still. Follow every fixture, result and scorer on the official site.', '2026-08-08 20:00:00+00'),\n  (2, 'Matchday 4: the table tightens', 'Four rounds complete and the top of the table is a fight. Goal difference is already deciding places — every finish matters.', '2026-08-29 20:30:00+00'),\n  (3, 'Midweek live: Talent host Pluto', 'A midweek fixture has been added to Matchday 5. Talent FC welcome Pluto FC at Talent Park. Live score updates will run from kickoff.', '2026-09-01 12:00:00+00'),\n  (4, 'Disciplinary note', 'The league reminds clubs that red-card suspensions are served in the next league fixture. Managers must check availability before naming a squad.', '2026-08-22 09:00:00+00');\nselect setval('announcements_id_seq', 4);\n";
/**
* Migration bookkeeping shared by the two appliers — `scripts/migrate.mjs`
* (deploy, `readdir`) and `src/lib/db.ts` (PGLite preview, `import.meta.glob`).
*
* Applied files are keyed by BASENAME, so the same file applies once no matter
* which directory it is globbed from. That is what makes the auth schema safe to
* copy from `migrations/auth/` into `migrations/` when an app turns sign-in on:
* a database that already has `0001_auth.sql` will not re-run it.
*
* Neither applier descends into subdirectories, so `migrations/auth/*.sql` is
* out of scope for both until it is copied up.
*/
/**
* The `_migrations` key for a migration path (or bare filename).
* @param {string} path
* @returns {string}
*/
function migrationName(path) {
	return path.split("/").pop() ?? path;
}
/**
* @param {string} path
* @returns {boolean}
*/
function isMigrationFile(path) {
	return path.endsWith(".sql");
}
/**
* Migrations in `paths` that are not yet in `applied`, in apply order.
* Non-`.sql` entries (a `readdir` also yields `migrations/auth/`) are dropped.
* @param {Iterable<string>} paths
* @param {Iterable<string>} applied
* @returns {Array<{ name: string, path: string }>}
*/
function pendingMigrations(paths, applied) {
	const done = new Set(applied);
	return [...paths].filter(isMigrationFile).map((path) => ({
		name: migrationName(path),
		path
	})).sort((a, b) => a.name.localeCompare(b.name)).filter(({ name }) => !done.has(name));
}
var rawDatabaseUrl = typeof process !== "undefined" ? process.env.DATABASE_URL : void 0;
var databaseUrl = rawDatabaseUrl && rawDatabaseUrl.trim() ? rawDatabaseUrl : void 0;
/**
* Active backend: real **Neon** when `DATABASE_URL` is set (deployed / configured
* sandbox), otherwise a local embedded **PGLite** (Postgres compiled to WASM) so
* the app has a working database even with nothing configured — the live preview
* included. Swap in Neon later by just setting `DATABASE_URL`; no code changes.
*/
var dbSource = databaseUrl ? "neon" : "pglite";
/**
* Init state lives on globalThis as promises: dev HMR creates new instances of
* this module, and two instances racing module-level state would open a second
* pool or run two concurrent PGLite migration passes (whose duplicate
* `_migrations` insert rejects — and would get memoized, poisoning every later
* `getSql()`). A failed init clears its slot so the next call retries.
*/
var globalRef = globalThis;
/**
* Result-type parity: Postgres sends every value as text plus a type OID — the
* JS value is the DRIVER's parsing choice, and pg and PGLite disagree (pg:
* int8 -> string, date -> local-midnight Date; PGLite: int8 -> BigInt, which
* JSON.stringify rejects, date -> UTC Date). Normalize both so preview and
* production return identical, JSON-safe shapes:
*   int8/bigint (incl. count(*)) -> number (past 2^53 loses precision — cast
*                                   `::text` if you ever need huge integers)
*   date                         -> 'YYYY-MM-DD' string
*   interval                     -> Postgres interval text
* numeric already comes back as a string on both (arbitrary precision).
*/
var OID_INT8 = 20;
var OID_DATE = 1082;
var OID_INTERVAL = 1186;
var identity = (v) => v;
/** Wrap a query runner in the tagged-template + `.query()` `Sql` surface. */
function toSql(run) {
	const sql = (async (strings, ...values) => {
		let text = strings[0];
		for (let i = 0; i < values.length; i += 1) text += `$${i + 1}${strings[i + 1]}`;
		return run(text, values);
	});
	sql.query = (text, params = []) => run(text, params);
	return sql;
}
function createNeonSql() {
	globalRef.__pgSqlPromise__ ??= (async () => {
		const { Pool, types } = await import("../_libs/pg.mjs").then((n) => n.n);
		types.setTypeParser(OID_INT8, Number);
		types.setTypeParser(OID_DATE, identity);
		types.setTypeParser(OID_INTERVAL, identity);
		const pool = new Pool({ connectionString: databaseUrl });
		return toSql(async (text, params) => {
			return (await pool.query(text, params)).rows;
		});
	})().catch((err) => {
		globalRef.__pgSqlPromise__ = void 0;
		throw err;
	});
	return globalRef.__pgSqlPromise__;
}
async function createPgliteSql() {
	globalRef.__pgliteInstance__ ??= (async () => {
		const { PGlite } = await import("../_libs/electric-sql__pglite.mjs").then((n) => n.t);
		const pg = new PGlite({ parsers: {
			[OID_INT8]: Number,
			[OID_DATE]: identity,
			[OID_INTERVAL]: identity
		} });
		await pg.waitReady;
		await pg.exec("create table if not exists _migrations (name text primary key, applied_at timestamptz not null default now())");
		return pg;
	})().catch((err) => {
		globalRef.__pgliteInstance__ = void 0;
		throw err;
	});
	const pg = await globalRef.__pgliteInstance__;
	const migrate = async () => {
		const migrations = /* #__PURE__ */ Object.assign({
			"/migrations/0001_auth.sql": _0001_auth_default,
			"/migrations/0002_league.sql": _0002_league_default,
			"/migrations/0003_seed.sql": _0003_seed_default
		});
		const done = (await pg.query("select name from _migrations")).rows.map((r) => r.name);
		for (const { name, path } of pendingMigrations(Object.keys(migrations), done)) await pg.transaction(async (tx) => {
			await tx.exec(migrations[path]);
			await tx.query("insert into _migrations (name) values ($1)", [name]);
		});
	};
	const pass = (globalRef.__pgliteMigrateChain__ ?? Promise.resolve()).catch(() => void 0).then(migrate);
	globalRef.__pgliteMigrateChain__ = pass;
	await pass;
	return toSql(async (text, params) => {
		return (await pg.query(text, params)).rows;
	});
}
var sqlPromise = null;
async function createSql() {
	if (typeof window !== "undefined") throw new Error("@/lib/db is server-only — call getSql() from a createServerFn handler or a server route loader, never from client code.");
	return dbSource === "neon" ? createNeonSql() : createPgliteSql();
}
/**
* Get the shared, **server-only** SQL client. Neon when `DATABASE_URL` is set,
* otherwise the local PGLite fallback. Memoized — safe to call per request.
*
* Schema comes from `migrations/*.sql`, auto-applied before the first query on
* both backends — define tables there, never inline in server functions.
*/
function getSql() {
	sqlPromise ??= createSql().catch((err) => {
		sqlPromise = null;
		throw err;
	});
	return sqlPromise;
}
/**
* The shared PGLite instance (preview only), with `migrations/*.sql` applied.
* Lets Better Auth persist to the SAME embedded DB as app data in preview (via a
* Kysely dialect). Throws when `DATABASE_URL` is set (that path uses Neon).
*/
async function getPglite() {
	if (dbSource !== "pglite") throw new Error("getPglite() is only available on the PGLite fallback (no DATABASE_URL)");
	await getSql();
	const pg = await globalRef.__pgliteInstance__;
	if (!pg) throw new Error("PGLite instance failed to initialize");
	return pg;
}
/**
* Finish DB bootstrap before the server handles traffic.
*
* - **PGLite** (preview / no `DATABASE_URL`): open the in-memory DB and apply
*   `migrations/*.sql`. Idempotent — concurrent callers share one promise.
* - **Neon**: no-op (pool is created lazily on first query).
*
* Vite `configureServer` awaits this at dev startup; production imports of this
* module kick it off immediately (see bottom of file).
*/
function ensureDbReady() {
	if (dbSource !== "pglite") return Promise.resolve();
	return getSql().then(() => void 0);
}
var globalBoot = globalThis;
if (typeof window === "undefined" && dbSource === "pglite") globalBoot.__pgBootstrapPromise__ ??= ensureDbReady().catch((err) => {
	globalBoot.__pgBootstrapPromise__ = void 0;
	console.error("[db] PGLite bootstrap failed:", err);
	throw err;
});
/**
* Auth middleware for server functions — the standard way to get the caller's
* verified user id. When deployed the session cookie is same-origin and rides
* along automatically. In the live preview the client also forwards the bearer
* token (partitioned cookies) via the `.client` hook below — call sites do not
* thread it themselves.
*
*   import { createServerFn } from "@tanstack/react-start";
*   import { getSql } from "@/lib/db";
*   import { authMiddleware } from "@/lib/auth/middleware";
*
*   export const listTodos = createServerFn({ method: "GET" })
*     .middleware([authMiddleware])
*     .handler(async ({ context }) => {
*       const sql = await getSql();
*       return sql`select * from todos where user_id = ${context.userId}`;
*     });
*
* Signed out with auth on (live preview included) -> throws `UnauthorizedError`
* (see `verify.server.ts`). With auth disabled (`VITE_AUTH_ENABLED=false`, the
* shipped default) it resolves the shared dev user — but throws instead when a
* `DATABASE_URL` is also set, so an app without sign-in must not use this at
* all. On the auth-on path, use it on every server function that touches
* per-user data and scope every query by `context.userId`.
*/
var authMiddleware = createMiddleware({ type: "function" }).client(async ({ next }) => {
	const { getBearerToken } = await import("./client-B40BzJxt.mjs").then((n) => n.n);
	return next({ sendContext: { bearerToken: getBearerToken() ?? void 0 } });
}).server(async ({ next, context }) => {
	const { assertSameSiteRequest } = await import("./isolation.server-CGNg1r0B.mjs");
	const { requireUserId } = await import("./verify.server-DmbFV0Zt.mjs");
	assertSameSiteRequest();
	return next({ context: { userId: await requireUserId(context.bearerToken) } });
});
function cn(...inputs) {
	return twMerge(clsx(inputs));
}
function toIso(value) {
	if (value instanceof Date) return value.toISOString();
	if (typeof value === "string") return value;
	return String(value ?? "");
}
function formatKickoff(iso) {
	const d = new Date(iso);
	if (Number.isNaN(d.getTime())) return {
		date: "—",
		time: "—",
		full: "—"
	};
	const date = new Intl.DateTimeFormat("en-GB", {
		weekday: "short",
		day: "numeric",
		month: "short"
	}).format(d);
	const time = new Intl.DateTimeFormat("en-GB", {
		hour: "2-digit",
		minute: "2-digit"
	}).format(d);
	return {
		date,
		time,
		full: `${date} · ${time}`
	};
}
function signed(n) {
	if (n > 0) return `+${n}`;
	return String(n);
}
//#endregion
export { getPglite as a, toIso as c, formatKickoff as i, cn as n, getSql as o, ensureDbReady as r, signed as s, authMiddleware as t };
